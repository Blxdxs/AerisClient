# Aeris Client Website

The supplied `logo.png` is used in the top-left header on every page and inside the interactive dashboard preview.

The logo is styled to match the glass-themed blue/cyan UI.

Dashboard header: the logo has no colored box behind it and the label reads "Aeris".
